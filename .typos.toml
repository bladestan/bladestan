[files]
extend-exclude = [
    ".git/",
]
ignore-hidden = false

[default.extend-identifiers]
"Symplify" = "Symplify"
"symplify" = "symplify"

# Deliberate typo used in the README as an example of a mistake Bladestan reports.
"emial" = "emial"

# Typos
"Github" = "GitHub"
